# Cloudflare Pages redirects
# Handle unit conversion URLs and SPA routing
/convert/units/*/index.html /convert/units/*/index.html 200

# Fallback for any missing pages (optional - uncomment if needed)
# /* /index.html 200

